import React from 'react';
import Navigation from '../components/Navigation';

const Teams = () => {
    return (
        <div>
            <Navigation />
        </div>
    );
};

export default Teams;